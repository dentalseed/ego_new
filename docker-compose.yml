version: "3"

services:
  web01:
    container_name: ego-web
    hostname: ego-web
    ports:
      - "9003:8080"
    build:
      context: ./
    deploy:
      mode: replicated
      replicas: 1
    networks:
      - web


networks: # 가장 기본적인 bridge 네트워크
  web:
    driver: bridge
